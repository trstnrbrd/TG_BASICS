# ── CREDENTIALS / SENSITIVE CONFIG ──────────────────────────────────────────
# These files contain DB passwords, SMTP credentials, etc.
# Copy the .example versions and fill in real values locally.
config/db.php
config/mailer.php
config/ocr.php

# ── ENVIRONMENT FILES ─────────────────────────────────────────────────────────
.env
.env.*
.env.local

# ── UPLOADED FILES (user-generated, confidential) ────────────────────────────
uploads/avatars/*
!uploads/avatars/.gitkeep
uploads/claims/*
!uploads/claims/.gitkeep
uploads/repair_jobs/*
!uploads/repair_jobs/.gitkeep
uploads/client_docs/*
!uploads/client_docs/.gitkeep
uploads/receipts/*
!uploads/receipts/.gitkeep

# ── VENDOR / DEPENDENCIES ────────────────────────────────────────────────────
vendor/

# ── PHPUNIT ───────────────────────────────────────────────────────────────────
.phpunit.result.cache

# ── XAMPP / OS FILES ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── PHP / SERVER LOGS ─────────────────────────────────────────────────────────
*.log
error_log

# ── EDITOR / IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo


# ── DATABASE DUMPS (never commit real data) ──────────────────────────────────
database/*.sql
*.sql

# ── TESTS / DOCUMENTATION (internal only) ────────────────────────────────────
data_gathering (documentation)/Full interview transcript.txt
data_gathering (documentation)/MANUSCRIPT.txt
selenium_tests/
tools/
tests/
.claude/
